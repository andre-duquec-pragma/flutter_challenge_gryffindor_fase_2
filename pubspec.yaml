name: flutter_challenge_gryffindor_fase_2
description: "A FakeStore API request project"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # External libraries
  dartz: ^0.10.1
  dio: ^5.4.3+1
  get_it: ^7.7.0
  logger: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
